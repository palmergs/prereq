{{#layout.page-header}}

{{/layout.page-header}}
<div id="flash">
  <div class="container-fluid">
    {{#if (gt flashService.queue.length 0) }}
      {{#each flashService.queue as |flash|}}
        {{#bs-alert type=flash.type}}{{flash.message}}{{/bs-alert}}
      {{/each}}
    {{/if}}
  </div>
</div>
<div class="container-fluid">
  <div class="row">
    <div class="col-sm-12">
      <h1>Welcome!</h1>
    </div>
  </div>
</div>
{{outlet}}
